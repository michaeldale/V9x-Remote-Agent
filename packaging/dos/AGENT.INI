[agent]
; TCP port the agent listens on (default 9869).
port=9869
; Restrict access to a single client IP (dotted quad). Blank = allow any host
; that can reach the port. The protocol is unauthenticated, so keep the NIC on
; an isolated network (e.g. an 86Box NAT) regardless.
allowed_client=
